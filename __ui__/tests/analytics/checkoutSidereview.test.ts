import { test } from '@Test';

test.describe('"CheckoutNonInteraction" "Error" events', () => {
    test('hui hui', async ({ page, context, CategoryPage }) => {
        await page.goto('/sunglasses');
    });
});
